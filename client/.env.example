# ZVote Authentication Configuration
# Copy this file to .env and fill in your OAuth credentials

# Google OAuth 2.0
# Get credentials from: https://console.cloud.google.com/
VITE_GOOGLE_CLIENT_ID=your-google-client-id-here.apps.googleusercontent.com

# Apple Sign In
# Get credentials from: https://developer.apple.com/
VITE_APPLE_CLIENT_ID=com.yourcompany.zvote

# Meta (Facebook) Login
# Get credentials from: https://developers.facebook.com/
VITE_META_CLIENT_ID=your-facebook-app-id-here

# Email Authentication (e.g., Auth0, Supabase)
# Optional: Configure if using email-based authentication
VITE_EMAIL_CLIENT_ID=your-email-auth-client-id
VITE_EMAIL_AUTH_ENDPOINT=https://your-domain.auth0.com/authorize
VITE_EMAIL_TOKEN_ENDPOINT=https://your-domain.auth0.com/oauth/token

# OAuth Redirect URI (should match your configured redirects)
# Development: http://localhost:5173/auth/callback
# Production: https://yourdomain.com/auth/callback

# Backend Token Exchange Endpoint (required for production)
# VITE_TOKEN_EXCHANGE_ENDPOINT=https://api.yourdomain.com/auth/token-exchange
