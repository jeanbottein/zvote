/*
  Legacy App.tsx kept for reference. The app now uses AppRouter via main.tsx and a Layout component.
  The legacy in-file views have been removed in favor of route-based pages.
*/

import AppRouter from './AppRouter';

export default function App() {
  return <AppRouter />;
}
